@extends('main')
@section('content')
<section class="main-content bg-light"><div class="container py-5"><div class="card border-0 shadow-sm mx-auto" style="max-width:720px;border-radius:18px"><div class="card-body p-4 p-lg-5"><h1 class="h4 font-weight-bold">رول میں ترمیم: {{ $role->name }}</h1><p class="text-muted">اس رول کے تمام ملازمین کو منتخب اجازتیں ملیں گی۔</p>@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form method="POST" action="{{ route('admin.team.roles.update', $role) }}">@csrf @method('PUT')<div class="form-group"><label>رول کا نام</label><input class="form-control" name="name" value="{{ old('name', $role->name) }}" required></div>@foreach($permissions as $key => $label)<label class="d-block border rounded p-3"><input type="checkbox" name="permissions[]" value="{{ $key }}" class="ml-2" {{ in_array($key, old('permissions', $role->permissions), true) ? 'checked' : '' }}>{{ $label }}</label>@endforeach<div class="d-flex mt-4"><button class="btn btn-primary ml-2">تبدیلی محفوظ کریں</button><a class="btn btn-outline-secondary" href="{{ route('admin.team.index') }}">واپس</a></div></form></div></div></div></section>
@endsection
